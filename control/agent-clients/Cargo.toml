[package]
name = "pontia-agent-clients"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
pontia-core = { path = "../core", default-features = false }
serde = { workspace = true, features = ["derive"] }
serde_json = { workspace = true }

[dev-dependencies]
tempfile = "3"

[features]
default = []
generic-test-client = []
