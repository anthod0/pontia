bind_addr = "127.0.0.1:8080"
external_api_token = "dev-token"
run_migrations = true

[dashboard]
# Local Vite dist directory or a remote .tar.gz/.tgz/.zip archive containing exactly one index.html.
source = "apps/dashboard/dist"
# Used only for remote sources. On startup pontia tries to refresh this cache;
# if refresh fails, an existing cached dashboard is served.
cache_dir = "~/.pontia/cache/dashboard"

[runtime.pi]
tui_command = "pi -e /absolute/path/to/pontia/clients/pi"


[workspace_browser]
roots = [
  { root_id = "projects", label = "Projects", path = "/home/me/projects" }
]
